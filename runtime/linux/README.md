# Linux Runtime Assets

This folder is reserved for portable Linux runtime artifacts.

Planned contents:
- rootfs bundle (Ubuntu-like userland)
- launcher helpers
- checksums and source manifests

Do not commit large binary runtime payloads directly until packaging policy is finalized.
